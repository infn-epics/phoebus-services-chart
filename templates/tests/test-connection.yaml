apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phoebus-services-chart.fullname" . }}-test-connection"
  labels:
    {{- include "phoebus-services-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phoebus-services-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
